# GitPowerAdd_
When executed from the git repository folder w/powershell it will: pull recent work, add all stuff in folder, commit it with the first parameter as the name, then push everything to github while suppressings errors (speciohically one where powershell writes to OUTERR file and displays it for whatever reason)
